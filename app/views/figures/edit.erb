<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit the Historical Figure</title>
  </head>
  <body>
    <h1>Edit the Historical Figure</h1>
    <%= @figure.name %>
    <form action="/figures/<%= @figure.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">

      <h2>Edit Name:</h2>

      <input id="figure_name" type="text" name="figure[name]" value="<%=@figure.name %>">

      <br></br>

      <h2>Edit Titles:</h2>

      <br></br>
      <% Title.all.each do |title| %>
      <input id="title_<%=title.id %>" type="checkbox" name="figure[title_ids][]" value="<%= title.id %>"><% 'checked' if @figure.titles.include?(title) %> <%= title.name %></input>
      <% end %>

      <br></br>

      <label>Custom Title!:</label>
      <input id="title_name" type="string" name="title[name]">

      <br></br>

      <label>Change da landmarks!:</label>
 +
 +    <% Landmark.all.each do |landmark| %>
 +      <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%= 'checked' if @figure.landmarks.include?(landmark) %>>
 +      <%= landmark.name %></input>
 +    <% end %>
 +
 +  <br><br>
 +
  <label>Create new landmark:</label>
 +
    <br></br>
 +  <input id="new_landmark" type="text" name="landmark[name]">
 +
 +  <br><br>
 +  <input type="submit" value="Edit Figure">

    </form>


  </body>
</html>
